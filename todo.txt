In frontend create a global state for uncaught error with name (Error, setError). to catch error which will be thrown by throw new error.
Implement a custom rateLimit or better rateLimit package in auth routes.
In backend, if the token is modified by a malicious user, what will be the error thrown by the jwt, and based on that i have to write a if conditinal error statement to throw an error.
if a user clicks verify button multiple times, there should not be a race condition, and in verify route there should be a rate limit or try limit to prevent brute force attacks.
implement TTL for verification token, means when a verification token gets expired, that means that session is expired. so the document. //think about it before implementing.
for now in phone input login form, user can input any number they want, and frontend take it as a hole number, unlike the whatsapp same feature. whatsapp convert the first nubmer to the country code and rest to the phone number.
In frontend showCard component, implement text color logic for unseen message and batch
change the toLocalTimeString or anything to intl.dateTimeformat for efficiency at  formatMessageTime method in frontend/lib.js
implement yup validation on input box also, to verify a user is not sending a malicious script. another reason to implement usehookform, because every time i type something inputbox component get re-render because of setmsgTxt.
move the userInfo state to the appStore from authStore
again i have to write the auth controller, mainly because of the userInfo data flow and overall the flow of the auth system, now its a little complicated in sending otp, receiving otp, and resend otp
use 'AsYouType' method of lib-phonenumber.js to parse and format a phone number as user type just like the original whatsapp
before deploy to production change the math.random() function with crypto.randomInt() for otp generation. Also read about the problem with using math.random() function in real world scenarios
change jwt's payload to _id from current's phone. as _id query are faster by default in mongodb. This is gonna be a huge update as we have update the controllers also.
implement mimeType checker and limit of uploads in multer and file uploading for profile into the server. for now there is no safety checker to check the mimetype or the magic number of a file. so malicious user can send anything they want.
In maincontent component inside useEffect scrollIntoView's behavior is set according to isFirstPage state, with this problem is that except the first time fetching messages it don't call the fetchNextPage method, but with the second render and forward it call the fetchNextPage method by which our pagination doesn't work as expected, our messages array instantly reaches our full messages length. The solution is for now i will implement a debounce function to prevent calling the fetchNextPage method for some second, but it is not the optimal solution, for that i hope when i maintain the user's scroll height as production application does, i hope gets solved. otherwise we have to look into it.